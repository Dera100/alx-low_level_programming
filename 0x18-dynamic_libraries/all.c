#include <stdio.h>
/**
 * add - add two integards kkk
 * @a: first int code area
 * @b: secound int code
 * Return: addition
 *
 */
int add(int a, int b)
{
	return (a + b);
}
/**
 * sub - subtract two integards kkk
 * @a: first int code area
 * @b: secound int code
 * Return: additionsubtract final result
 *
 */
int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - mul two integards kkk
 * @a: first int code area
 * @b: secound int code
 * Return: multiply
 *
 */
int mul(int a, int b)
{
	return (a * b);
}
/**
 * mod - mod two integards kkk
 * @a: first int code area
 * @b: secound int code
 * Return: mod
 *
 */
int mod(int a, int b)
{
	return (a % b);
}
/**
 * div - div two integards kkk
 * @a: first int code area
 * @b: secound int code
 * Return: division result
 *
 */
int div(int a, int b)
{
	return (a / b);
}
